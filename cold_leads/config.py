"""
config.py — Конфигурация системы холодных лидов агентства "Динамика"
Все настройки в одном месте: API-ключи, параметры ЦКП, ограничения.
"""

import os
from dataclasses import dataclass, field
from typing import List


# ---------------------------------------------------------------------------
# API Keys (из переменных окружения — никогда не хардкодим)
# ---------------------------------------------------------------------------

ANTHROPIC_API_KEY: str = os.environ.get("ANTHROPIC_API_KEY", "")
ANTHROPIC_BASE_URL: str = os.environ.get("ANTHROPIC_BASE_URL", "")
TWOGIS_API_KEY: str = os.environ.get("TWOGIS_API_KEY", "")
TWOGIS_DEMO_KEY: str = "demo"   # fallback demo-ключ 2GIS
VK_ACCESS_TOKEN: str = os.environ.get("VK_ACCESS_TOKEN", "")

# Bitrix24 webhook URL для передачи лидов в CRM
BITRIX24_WEBHOOK_URL: str = os.environ.get("BITRIX24_WEBHOOK_URL", "")

# Яндекс Диск — OAuth-токен для загрузки PDF-КП
YANDEX_DISK_TOKEN: str = os.environ.get("YANDEX_DISK_TOKEN", "")


# ---------------------------------------------------------------------------
# Агентство
# ---------------------------------------------------------------------------

AGENCY_NAME = "Динамика"
AGENCY_DOMAIN = "dynamicbrands.ru"
AGENCY_WEBSITE = "dynamicbrands.ru"
AGENCY_CITY = "Пермь"

# Тарифы (RUB/месяц)
TARIFF_MIN = 43_600
TARIFF_MAX = 200_000
TARIFF_ONBOARDING = 45_000  # разовый платёж за онбординг

# Детальный справочник тарифов (используется qualifier, profiler, message_generator)
TARIFF_TIERS: dict = {
    "БАЗОВЫЙ": {
        "price": 43_600,
        "price_min": 43_600,
        "price_max": 75_700,
        "duration": "от 3 месяцев",
        "description": "Ведение соцсетей, контент-план, оформление страниц",
        "best_for": "малый бизнес без соцсетей или с неактивными страницами",
    },
    "КОРОТКАЯ ВОРОНКА": {
        "price": 85_000,
        "price_min": 85_000,
        "price_max": 85_000,
        "duration": "от 2 месяцев",
        "description": "Воронка быстрых продаж: таргет + контент + прогрев",
        "best_for": "бизнес с имеющейся аудиторией, нужны быстрые заявки",
    },
    "ПРОГРЕВАЮЩАЯ ВОРОНКА": {
        "price": 120_000,
        "price_min": 120_000,
        "price_max": 120_000,
        "duration": "от 4 месяцев",
        "description": "Полная воронка прогрева: контент + экспертность + доверие + продажи",
        "best_for": "сложные продукты с долгим циклом принятия решения",
    },
    "SEO-ВОРОНКА": {
        "price": 90_000,
        "price_min": 90_000,
        "price_max": 90_000,
        "duration": "от 6 месяцев",
        "description": "SEO + SMM: органический трафик + социальное доказательство",
        "best_for": "бизнес, которому нужен долгосрочный трафик без рекламы",
    },
    # Алиасы для обратной совместимости с rule-based fallback qualifier
    "Старт": {
        "price": 43_600,
        "price_min": 43_600,
        "price_max": 75_700,
        "duration": "от 3 месяцев",
        "description": "Базовый пакет SMM",
        "best_for": "старт присутствия в соцсетях",
    },
    "Бизнес": {
        "price": 89_000,
        "price_min": 85_000,
        "price_max": 90_000,
        "duration": "от 2 месяцев",
        "description": "SMM + воронка продаж",
        "best_for": "активный рост аудитории и продаж",
    },
}


# ---------------------------------------------------------------------------
# 2GIS API
# ---------------------------------------------------------------------------

TWOGIS_API_BASE = "https://catalog.api.2gis.com/3.0/items"
TWOGIS_CITY_ID = "38"  # ID Перми в 2GIS

# Целевые категории для парсинга
TARGET_CATEGORIES: List[str] = [
    "медицина",
    "фитнес",
    "красота",
    "общепит",
    "розничная торговля",
    "строительство и ремонт",
    "образование",
]

# Дополнительные поисковые запросы для расширения охвата
TARGET_SEARCH_QUERIES: List[str] = [
    "ресторан Пермь",
    "кафе Пермь",
    "салон красоты Пермь",
    "фитнес-клуб Пермь",
    "стоматология Пермь",
    "медицинский центр Пермь",
    "спортивный клуб Пермь",
    "йога Пермь",
    "барбершоп Пермь",
    "ногтевая студия Пермь",
    "пекарня Пермь",
    "кондитерская Пермь",
    "интерьер Пермь",
    "мебель Пермь",
    "дизайн интерьера Пермь",
    "детский центр Пермь",
    "языковая школа Пермь",
    "танцевальная студия Пермь",
]

# Поля, запрашиваемые у 2GIS API
TWOGIS_FIELDS = (
    "items.point,"
    "items.contact_groups,"
    "items.links,"
    "items.reviews,"
    "items.rubrics,"
    "items.address,"
    "items.name_ex"
)

TWOGIS_PAGE_SIZE = 10  # Бесплатный тариф 2GIS: макс. 10 на страницу


# ---------------------------------------------------------------------------
# ICP — Идеальный профиль клиента
# ---------------------------------------------------------------------------

@dataclass
class ICPConfig:
    # Минимальный рейтинг для HIGH-приоритета
    min_rating: float = 4.0

    # Минимальное количество отзывов для HIGH-приоритета
    min_reviews: int = 10

    # Если последний пост в соцсетях был раньше N дней назад — флаг "неактивный"
    inactive_social_days: int = 21  # 3 недели

    # Визуальные ниши (HIGH-приоритет)
    visual_niches: List[str] = field(default_factory=lambda: [
        "еда", "питание", "ресторан", "кафе", "кофейня", "пекарня",
        "красота", "салон", "косметология", "барбершоп", "маникюр",
        "фитнес", "спорт", "йога", "танцы",
        "медицина", "стоматология", "клиника",
        "интерьер", "мебель", "дизайн",
        "дети", "образование",
    ])

    # Стоп-слова: если содержится в названии/рубрике — пропускаем
    skip_keywords: List[str] = field(default_factory=lambda: [
        "государственн",
        "муниципальн",
        "бюджетн",
        "фонд",
        "нко",
        "некоммерческ",
        "фрайшайз",
        "франшиз",
    ])

    # Крупные сети / франшизы (пропускаем)
    known_chains: List[str] = field(default_factory=lambda: [
        "мcdonald", "burger king", "kfc", "subway", "domino",
        "шоколадница", "coffeeshop", "costa coffee", "starbucks",
        "спортмастер", "декатлон", "fit service",
        "медси", "инвитро", "гемотест", "ситилаб",
    ])


ICP = ICPConfig()


# ---------------------------------------------------------------------------
# Rate limiting
# ---------------------------------------------------------------------------

# Максимум лидов в день (ограничение нагрузки и стоимости API)
MAX_LEADS_PER_DAY = 30

# Паузы между запросами (секунды)
TWOGIS_REQUEST_DELAY_MIN = 2.0
TWOGIS_REQUEST_DELAY_MAX = 3.5

SOCIAL_CHECK_DELAY_MIN = 1.0
SOCIAL_CHECK_DELAY_MAX = 2.0

CLAUDE_REQUEST_DELAY = 0.5  # пауза между запросами к Claude

# Повторные попытки при сетевых ошибках
MAX_RETRIES = 3
RETRY_BACKOFF = 1.5  # множитель backoff для urllib3


# ---------------------------------------------------------------------------
# Модель Claude
# ---------------------------------------------------------------------------

# Используем claude-sonnet-4-6 (как просил пользователь — claude-sonnet-4-5 / 3.5)
# Обновлено до актуального ID согласно текущим моделям
CLAUDE_MODEL = os.environ.get("CLAUDE_MODEL", "claude-opus-4-8-20251101")
CLAUDE_MAX_TOKENS = 1024


# ---------------------------------------------------------------------------
# База данных
# ---------------------------------------------------------------------------

DB_PATH = os.environ.get("COLD_LEADS_DB", "cold_leads.db")


# ---------------------------------------------------------------------------
# Логирование
# ---------------------------------------------------------------------------

LOG_LEVEL = os.environ.get("LOG_LEVEL", "INFO")
LOG_FILE = os.environ.get("LOG_FILE", "cold_leads.log")


# ---------------------------------------------------------------------------
# Валидация конфига при запуске
# ---------------------------------------------------------------------------

def validate_config() -> List[str]:
    """
    Проверяет наличие обязательных переменных окружения.
    Возвращает список предупреждений.
    """
    warnings = []
    if not ANTHROPIC_API_KEY:
        warnings.append("ANTHROPIC_API_KEY не задан — квалификация лидов через Claude недоступна")
    if TWOGIS_API_KEY == "demo":
        warnings.append("TWOGIS_API_KEY не задан — используется demo-ключ (лимиты ограничены)")
    if not VK_ACCESS_TOKEN:
        warnings.append("VK_ACCESS_TOKEN не задан — проверка VK через API недоступна")
    if not BITRIX24_WEBHOOK_URL:
        warnings.append("BITRIX24_WEBHOOK_URL не задан — синхронизация с CRM отключена")
    if not YANDEX_DISK_TOKEN:
        warnings.append("YANDEX_DISK_TOKEN не задан — PDF-КП не будут загружаться на Яндекс Диск")
    return warnings
